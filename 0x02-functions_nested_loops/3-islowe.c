#include <ctype.h>

/**
 * islower - checks if a character is lowercase
 * @c: the character to be checked
 *
 * Return: 1 if c is lowercase, 0 otherwise
 */
int islower(int c)
{
    /* Use the islower() function from ctype.h */
    return islower(c) != 0;
}

